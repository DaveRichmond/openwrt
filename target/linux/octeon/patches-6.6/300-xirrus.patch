diff --git a/arch/mips/cavium-octeon/executive/cvmx-helper-board.c b/arch/mips/cavium-octeon/executive/cvmx-helper-board.c
index 0fd9ac76eb74..39a25db16b1c 100644
--- a/arch/mips/cavium-octeon/executive/cvmx-helper-board.c
+++ b/arch/mips/cavium-octeon/executive/cvmx-helper-board.c
@@ -179,6 +179,18 @@ int cvmx_helper_board_get_mii_address(int ipd_port)
 			return 1;
 		else
 			return -1;
+	case CVMX_BOARD_TYPE_XIRRUS:
+		// unless I'm missing something, once we're using a devtree this no 
+		// longer applies (read: I don't see it getting called now that
+		// I've got a valid dtb file)
+		cvmx_dprintf("Xirrus IPD Port %d requested", ipd_port);
+		// FIXME: maybe? kinda guessing here
+		if (ipd_port >= 0 && ipd_port <= 3)
+			return ipd_port;
+		else if (ipd_port >= 16 && ipd_port <= 19)
+			return ipd_port - 16 + 4;
+		else
+			return -1;
 
 	}
 
diff --git a/arch/mips/cavium-octeon/setup.c b/arch/mips/cavium-octeon/setup.c
index 1ad2602a0383..d5706b103983 100644
--- a/arch/mips/cavium-octeon/setup.c
+++ b/arch/mips/cavium-octeon/setup.c
@@ -919,6 +919,16 @@ void __init prom_init(void)
 #endif
 #endif
 
+	// xirrus use the board revision to specify which model they're using
+	// this is a handy place to just make sure we're finding what we expect
+	if(sysinfo->board_type == CVMX_BOARD_TYPE_XIRRUS){
+		if(sysinfo->board_rev_major == BD_X2_120){
+			pr_info("Xirrus X2-120 Detected");
+			pr_info("Board Type: %d", sysinfo->board_type);
+			pr_info("Board SN: %s", sysinfo->board_serial_number);
+		}
+	}
+
 	octeon_user_io_init();
 	octeon_setup_smp();
 }
@@ -1146,6 +1156,7 @@ void __init device_tree_init(void)
 	bool fill_mac;
 
 #ifdef CONFIG_MIPS_ELF_APPENDED_DTB
+	pr_info("Looking for appended Device Tree\n");
 	if (!fdt_check_header(&__appended_dtb)) {
 		fdt = &__appended_dtb;
 		do_prune = false;
diff --git a/arch/mips/include/asm/octeon/cvmx-bootinfo.h b/arch/mips/include/asm/octeon/cvmx-bootinfo.h
index c1c0b3230e0a..272c7f1f76ba 100644
--- a/arch/mips/include/asm/octeon/cvmx-bootinfo.h
+++ b/arch/mips/include/asm/octeon/cvmx-bootinfo.h
@@ -300,6 +300,7 @@ enum cvmx_board_types_enum {
 	CVMX_BOARD_TYPE_UBNT_E220 = 20005,
 	CVMX_BOARD_TYPE_CUST_DSR1000N = 20006,
 	CVMX_BOARD_TYPE_UBNT_E300 = 20300,
+	CVMX_BOARD_TYPE_XIRRUS = 20495,
 	CVMX_BOARD_TYPE_KONTRON_S1901 = 21901,
 	CVMX_BOARD_TYPE_CUST_PRIVATE_MAX = 30000,
 
@@ -405,6 +406,7 @@ static inline const char *cvmx_board_type_to_string(enum
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_UBNT_E220)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_CUST_DSR1000N)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_UBNT_E300)
+		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_XIRRUS)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_KONTRON_S1901)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_CUST_PRIVATE_MAX)
 	}
diff --git a/arch/mips/include/asm/octeon/octeon.h b/arch/mips/include/asm/octeon/octeon.h
index 5c1d726c702f..d0b28270b1ca 100644
--- a/arch/mips/include/asm/octeon/octeon.h
+++ b/arch/mips/include/asm/octeon/octeon.h
@@ -97,6 +97,9 @@ struct octeon_boot_descriptor {
 	uint32_t spi_clock_hz;
 	uint16_t board_type;
 	uint8_t board_rev_major;
+// this is probably bad practice now, but xirrus had all their boards defined by major rev
+// and this is a reasable place to define them
+#define BD_X2_120 130 // Xirrus X2-120
 	uint8_t board_rev_minor;
 	uint16_t chip_type;
 	uint8_t chip_rev_major;
